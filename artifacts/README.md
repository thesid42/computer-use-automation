# Capability artifacts

Successful discovery runs write versioned, schema-validated capability JSON files here. Artifacts must contain parameter references rather than concrete sensitive values.

No artifact in this directory should be described as a genuine discovery artifact unless it was produced by a real live-model run against the browser surface.
